rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // No trustworthy role mapping is currently enforced by the application.
      // Keep client access closed until authorization rules and emulator tests
      // are introduced for the final identity model.
      allow read, write: if false;
    }
  }
}
